// The app shell. For now it is only a placeholder proving the scaffold runs;
// real screens arrive with the wizard work (see docs/PLANNING.md, build order).
function App() {
  return (
    <main>
      <h1>Practice Lineup Builder</h1>
      <p>Nothing here yet. The solver comes first.</p>
    </main>
  )
}

export default App
